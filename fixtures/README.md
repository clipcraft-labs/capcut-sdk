# Public fixtures

Only reviewed, synthetic or sanitised JSON fixtures belong here. Use
`python tools/sanitize_fixture.py INPUT.json OUTPUT.json` as a first pass, then
manually inspect the result before committing it. Raw captures and harness
runtime responses are intentionally ignored by the repository.

